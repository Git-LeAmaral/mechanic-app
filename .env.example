# Copie para .env e ajuste se necessário.
# Com Docker: docker compose up -d
# Usa porta 5433 no host (evita conflito com Postgres local na 5432).
DATABASE_URL="postgresql://mecaflow:mecaflow@localhost:5433/mecaflow?schema=public"
